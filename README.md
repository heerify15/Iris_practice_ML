# Iris
A machine learning classification project on the Iris dataset using Logistic Regression, k-Nearest Neighbors (kNN), and Gaussian Naive Bayes. The project includes data preprocessing, feature scaling, hyperparameter tuning with GridSearchCV, model evaluation using accuracy, precision, recall, F1-score, confusion matrices, and performance comparison.
